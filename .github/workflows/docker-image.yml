# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# GitHub recommends pinning actions to a commit SHA.
# To get a newer version, you will need to update the SHA.
# You can also reference a tag or branch, but the action may change without warning.

name: Create and publish a Docker image

on:
  push:
    branches: ['main']

jobs:
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.ref_name }}
      url: ${{ steps.deploy.outputs.vm_url }}
    steps:
      - name: Docker Pull & Run From Hub
        uses: appleboy/ssh-action@master
        with:
          host: ec2-16-170-37-208.eu-north-1.compute.amazonaws.com
          username: ec2-user
          key: ${{secrets.ACTION_PRIVATE_KEY}}
          envs: GITHUB_SHA
          script: |
            docker pull fdepalma95/my_page
            docker stop my_page
            docker rm my_page
            docker run --detach --name my_page -p 80:3000 fdepalma95/my_page